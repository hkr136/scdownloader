# 🚀 Быстрый старт - SoundCloud Telegram Bot

Запустите бота за 5 минут!

## Шаг 1: Подготовка

### Требования
- Python 3.8+
- Учетная запись Telegram

## Шаг 2: Получите токены

### Telegram Bot Token

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/newbot`
4. Придумайте имя бота (например: "My SoundCloud Bot")
5. Придумайте username (например: "my_soundcloud_bot")
6. **Скопируйте токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### SoundCloud Client ID

1. Откройте [soundcloud.com](https://soundcloud.com) в браузере
2. Нажмите **F12** (откроются Developer Tools)
3. Перейдите на вкладку **Network**
4. Включите любой трек на сайте
5. В списке запросов найдите `api-v2.soundcloud.com`
6. Кликните на запрос и найдите `client_id=...` в URL
7. **Скопируйте значение** client_id

## Шаг 3: Установка

### Linux/Mac

```bash
# Перейдите в папку проекта
cd soundcloud-downloader

# Создайте .env файл
cp .env.example .env

# Откройте файл для редактирования
nano .env
```

### Windows

```bash
# Перейдите в папку проекта
cd soundcloud-downloader

# Создайте .env файл
copy .env.example .env

# Откройте файл в блокноте
notepad .env
```

## Шаг 4: Настройка .env

Вставьте свои токены в `.env` файл:

```bash
# ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
SOUNDCLOUD_CLIENT_ID=ваш_client_id

# ОПЦИОНАЛЬНЫЕ (можно оставить по умолчанию)
DOWNLOAD_DIRECTORY=./downloads
TEMP_DIRECTORY=./temp
MAX_FILE_SIZE_MB=50
LOG_LEVEL=INFO
```

Сохраните файл.

## Шаг 5: Запуск

### Вариант A: Скрипт (Linux/Mac)

```bash
./run.sh
```

### Вариант B: Вручную

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python main.py
```

### Вариант C: Docker

```bash
docker-compose up -d
```

## Шаг 6: Используйте бота!

1. Откройте Telegram
2. Найдите своего бота по username
3. Нажмите **Start** или отправьте `/start`
4. Отправьте боту ссылку на трек с SoundCloud
5. Получите музыку! 🎵

## Пример использования

1. Найдите трек на SoundCloud:
   ```
   https://soundcloud.com/artist/song-name
   ```

2. Скопируйте ссылку

3. Отправьте боту в Telegram

4. Бот покажет информацию о треке:
   ```
   📊 Информация о треке:
   🎵 Название: Song Name
   👤 Исполнитель: Artist
   ⏱ Длительность: 3:45
   
   ⬇️ Начинаю загрузку...
   ```

5. Получите аудио файл!

## Частые вопросы

**Q: Бот не отвечает**
- Проверьте, что `main.py` запущен
- Проверьте логи: `tail -f bot.log`
- Убедитесь, что токен правильный

**Q: "Invalid configuration"**
- Проверьте `.env` файл
- Убедитесь, что оба токена указаны

**Q: "Failed to resolve URL"**
- Проверьте, что ссылка правильная
- Убедитесь, что трек публично доступен

**Q: Файл слишком большой**
- Telegram ограничивает размер до 50MB
- Измените `MAX_FILE_SIZE_MB` в `.env`

## Остановка бота

### Если запустили через скрипт или вручную
Нажмите **Ctrl+C**

### Если запустили через Docker
```bash
docker-compose down
```

## Что дальше?

- Прочитайте полную документацию в [README.md](README.md)
- Настройте дополнительные параметры в `.env`
- Настройте автозапуск через systemd (Linux)
- Деплойте на сервер

Наслаждайтесь музыкой! 🎵
