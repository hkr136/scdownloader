# 🚀 Инструкция по миграции на сервер (Client ID Rotation)

## 📋 Обзор

Эта инструкция описывает процесс обновления бота с ротацией Client ID на production сервере.

## ⚠️ Важно перед началом

- ✅ Сделайте backup текущего `.env` файла
- ✅ Получите 2-3 Client ID заранее
- ✅ Выберите время с минимальной нагрузкой
- ✅ Убедитесь что у вас есть доступ к серверу

## 🔄 Миграция с Docker (рекомендуется)

### Шаг 1: Подготовка на локальной машине

```bash
# Убедитесь что вы на правильной ветке
git checkout feature/client-id-rotation

# Проверьте что все тесты проходят
python -m pytest tests/test_client_id_manager.py -v

# Создайте архив для сервера (опционально)
git archive --format=tar.gz --output=bot-rotation-update.tar.gz HEAD
```

### Шаг 2: Backup на сервере

```bash
# Подключитесь к серверу
ssh user@your-server

# Перейдите в директорию бота
cd /path/to/soundcloud-bot

# Создайте backup
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)
docker-compose logs --tail=100 > logs_before_update.txt

# Backup текущей версии (опционально)
tar -czf backup-$(date +%Y%m%d_%H%M%S).tar.gz src/ main.py .env
```

### Шаг 3: Остановка бота

```bash
# Остановите контейнер
docker-compose down

# Или если используется systemd
sudo systemctl stop soundcloud-bot
```

### Шаг 4: Обновление кода

**Вариант A: Через Git**

```bash
# Получите последние изменения
git fetch origin

# Переключитесь на ветку с ротацией
git checkout feature/client-id-rotation

# Или merge в main (если уже смержили)
# git checkout main
# git pull origin main
```

**Вариант B: Загрузка архива**

```bash
# Скачайте архив на сервер
scp bot-rotation-update.tar.gz user@your-server:/path/to/soundcloud-bot/

# На сервере распакуйте
tar -xzf bot-rotation-update.tar.gz
```

### Шаг 5: Обновление .env

Обновите `.env` файл, добавив новые параметры:

```bash
# Откройте .env
nano .env
```

Измените/добавьте:

```env
# Старый способ (закомментируйте или удалите)
# SOUNDCLOUD_CLIENT_ID=single_id_here

# Новый способ - несколько ID
SOUNDCLOUD_CLIENT_IDS=id1,id2,id3

# Настройки ротации (опционально)
CLIENT_ID_ROTATION_STRATEGY=failover
CLIENT_ID_COOLDOWN_SECONDS=300
```

**💡 Совет:** Если хотите сначала протестировать, оставьте старый `SOUNDCLOUD_CLIENT_ID` - он будет работать.

### Шаг 6: Пересборка Docker образа

```bash
# Пересоберите образ с новым кодом
docker-compose build

# Или с --no-cache для чистой сборки
docker-compose build --no-cache
```

### Шаг 7: Запуск бота

```bash
# Запустите обновленный бот
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

### Шаг 8: Проверка работы

Ищите в логах:

```
✅ Успешный запуск:
[INFO] ClientIDManager initialized with 3 client IDs, strategy: failover
[INFO] ✅ Configuration validated successfully
[INFO] Bot started successfully!

✅ Успешные запросы:
[DEBUG] Making request to: ... with client_id: abc12345...
[DEBUG] Client ID abc12345... marked as successful (total: 1)

⚠️ Ротация работает:
[WARN] Authentication failed with client_id abc12345... (status: 401)
[INFO] Attempting retry with new client ID...
[INFO] Switched to client_id #2 (def67890...)
```

### Шаг 9: Тестирование

```bash
# Отправьте боту тестовую ссылку
# Проверьте что трек скачивается

# Следите за логами в реальном времени
docker-compose logs -f soundcloud-bot
```

## 🔧 Миграция без Docker

### Шаг 1-4: Аналогично Docker варианту

### Шаг 5: Обновление зависимостей

```bash
# Активируйте venv
source venv/bin/activate

# Обновите зависимости (если нужно)
pip install -r requirements.txt --upgrade
```

### Шаг 6: Обновление .env

```bash
nano .env
# Добавьте SOUNDCLOUD_CLIENT_IDS как описано выше
```

### Шаг 7: Перезапуск бота

**Если используется systemd:**

```bash
sudo systemctl restart soundcloud-bot
sudo systemctl status soundcloud-bot
journalctl -u soundcloud-bot -f
```

**Если используется screen/tmux:**

```bash
# Остановите текущий процесс (Ctrl+C в screen)
# Или kill процесс

# Запустите снова
screen -S soundcloud-bot
python main.py
# Ctrl+A, D для detach
```

**Если используется supervisor:**

```bash
sudo supervisorctl restart soundcloud-bot
sudo supervisorctl status soundcloud-bot
sudo supervisorctl tail -f soundcloud-bot
```

## 🔍 Проверка после миграции

### 1. Проверьте логи

```bash
# Docker
docker-compose logs --tail=50 soundcloud-bot

# Systemd
journalctl -u soundcloud-bot -n 50

# Файл логов
tail -f bot.log
```

### 2. Проверьте функциональность

- [ ] Бот отвечает на `/start`
- [ ] Бот скачивает треки
- [ ] Логи показывают инициализацию ClientIDManager
- [ ] Нет ошибок при запуске

### 3. Проверьте ротацию (опционально)

Для тестирования ротации можете временно добавить невалидный ID первым:

```env
SOUNDCLOUD_CLIENT_IDS=invalid_test_id,real_id1,real_id2
```

Отправьте боту ссылку и убедитесь что он переключается на real_id1.

## 🚨 Откат в случае проблем

### Docker

```bash
# Остановите текущую версию
docker-compose down

# Вернитесь на предыдущую версию
git checkout main  # или предыдущий коммит

# Восстановите .env
cp .env.backup.YYYYMMDD_HHMMSS .env

# Пересоберите и запустите
docker-compose build
docker-compose up -d
```

### Без Docker

```bash
# Остановите бота
sudo systemctl stop soundcloud-bot

# Вернитесь на старую версию
git checkout main

# Восстановите .env
cp .env.backup.YYYYMMDD_HHMMSS .env

# Перезапустите
sudo systemctl start soundcloud-bot
```

## 📊 Мониторинг

### Что проверять регулярно

```bash
# Статус контейнера
docker-compose ps

# Использование ресурсов
docker stats soundcloud-telegram-bot

# Логи последние 24 часа
docker-compose logs --since 24h

# Поиск ошибок
docker-compose logs | grep ERROR
docker-compose logs | grep "All client IDs are exhausted"
```

### Алерты

Настройте мониторинг на следующие события:

- `[ERROR] All client IDs are exhausted!` - нужно добавить новые ID
- Частые переключения ID - возможно все ID скоро станут неактивными
- Рост использования памяти

## 📝 Checklist после миграции

- [ ] Бот успешно запущен
- [ ] ClientIDManager инициализирован
- [ ] Логи не показывают ошибок
- [ ] Бот отвечает на команды
- [ ] Бот скачивает треки
- [ ] Ротация работает (если тестировали)
- [ ] Backup старой версии сохранен
- [ ] Документация обновлена
- [ ] Команда уведомлена об обновлении

## 🆘 Troubleshooting

### Проблема: "No module named 'client_id_manager'"

**Решение:**
```bash
# Убедитесь что все файлы скопированы
ls -la src/api/client_id_manager.py

# Пересоберите Docker образ
docker-compose build --no-cache
```

### Проблема: "No active client IDs available"

**Решение:**
```bash
# Добавьте новые client_id в .env
nano .env
# Добавьте рабочие ID

# Перезапустите
docker-compose restart
```

### Проблема: Бот не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs soundcloud-bot

# Проверьте .env файл
cat .env | grep SOUNDCLOUD_CLIENT

# Проверьте синтаксис
python -m py_compile src/api/client_id_manager.py
```

## 📞 Контакты

При проблемах:
1. Проверьте [TESTING_ROTATION.md](docs/TESTING_ROTATION.md)
2. Изучите логи бота
3. Создайте issue в репозитории

## 🎯 Best Practices

1. **Всегда делайте backup** перед обновлением
2. **Тестируйте локально** перед деплоем на production
3. **Используйте минимум 3 Client ID** для надежности
4. **Мониторьте логи** первые 24 часа после обновления
5. **Обновляйте Client ID регулярно** (раз в 1-2 недели)

---

**Версия:** 1.0  
**Дата:** 2026-01-10  
**Ветка:** feature/client-id-rotation
