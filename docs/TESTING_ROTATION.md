# 🧪 Тестирование Client ID Rotation

## Быстрое тестирование

### 1. Запуск unit тестов

```bash
python -m pytest tests/test_client_id_manager.py -v
```

Ожидаемый результат: все 21 тест должны пройти успешно.

### 2. Тестирование с реальным ботом

#### Шаг 1: Настройка тестового окружения

Создайте `.env` файл с несколькими client_id:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
SOUNDCLOUD_CLIENT_IDS=id1,id2,id3
CLIENT_ID_ROTATION_STRATEGY=failover
CLIENT_ID_COOLDOWN_SECONDS=60
```

#### Шаг 2: Запуск бота

```bash
python main.py
```

Проверьте логи при запуске:
```
[INFO] ClientIDManager initialized with 3 client IDs, strategy: failover
```

#### Шаг 3: Тестирование базовой функциональности

1. Отправьте боту ссылку на трек SoundCloud
2. Проверьте, что трек скачивается успешно
3. Посмотрите в логах:
   ```
   [DEBUG] Making request to: https://api-v2.soundcloud.com/resolve with client_id: abc12345...
   [DEBUG] Client ID abc12345... marked as successful (total: 1)
   ```

#### Шаг 4: Тестирование failover

**Способ 1: Симуляция невалидного ID**

1. Добавьте в `.env` один заведомо неправильный ID:
   ```env
   SOUNDCLOUD_CLIENT_IDS=invalid_id,real_id_1,real_id_2
   ```

2. Перезапустите бота и отправьте ссылку

3. Проверьте логи - должно быть автоматическое переключение:
   ```
   [WARN] Authentication failed with client_id invalid_i... (status: 401)
   [INFO] Attempting retry with new client ID...
   [INFO] Switched to client_id #2 (real_id_...)
   ```

**Способ 2: Удаление ID из первой позиции**

1. После нескольких успешных запросов замените первый ID на невалидный
2. Перезагрузите бота
3. Отправьте новую ссылку
4. Наблюдайте автоматическое переключение

### 3. Тестирование round-robin стратегии

```env
CLIENT_ID_ROTATION_STRATEGY=round-robin
```

Отправьте несколько треков подряд и проверьте логи:
```
[DEBUG] ... with client_id: abc12345...
[DEBUG] ... with client_id: def67890...
[DEBUG] ... with client_id: ghi01234...
[DEBUG] ... with client_id: abc12345...  # цикл повторяется
```

### 4. Тестирование cooldown механизма

1. Установите короткий cooldown:
   ```env
   CLIENT_ID_COOLDOWN_SECONDS=10
   ```

2. Добавьте невалидный ID первым
3. Подождите 10 секунд после первой ошибки
4. Отправьте новую ссылку
5. Система попробует использовать "восстановленный" ID

## Интеграционные тесты

### Тест 1: Backward Compatibility

Проверяем, что старая конфигурация работает:

```env
# Удалите SOUNDCLOUD_CLIENT_IDS
SOUNDCLOUD_CLIENT_ID=single_id_here
```

Бот должен работать как раньше.

### Тест 2: Все ID невалидны

```env
SOUNDCLOUD_CLIENT_IDS=bad1,bad2,bad3
```

Ожидаемое поведение:
- Бот пытается использовать все ID
- Показывает ошибку пользователю после исчерпания пула
- Логирует: `[ERROR] All client IDs are exhausted!`

### Тест 3: Плейлисты

Отправьте ссылку на плейлист (5-10 треков):
- Все треки должны скачаться успешно
- ClientIDManager должен корректно работать при множественных запросах

### Тест 4: Concurrent запросы

Отправьте несколько ссылок одновременно от разных пользователей:
- Все запросы должны обработаться
- Не должно быть race conditions
- ClientIDManager должен быть thread-safe

## Проверка логов

### Успешные запросы

```
[INFO] ClientIDManager initialized with 3 client IDs, strategy: failover
[DEBUG] Making request to: ... with client_id: abc12345...
[DEBUG] Client ID abc12345... marked as successful (total: 1)
[INFO] Retrieved track: Artist - Title
```

### Failover при ошибке

```
[WARN] Authentication failed with client_id abc12345... (status: 401)
[WARN] Client ID abc12345... marked as failed (code: 401, status: failed, fail_count: 1)
[INFO] Attempting retry with new client ID...
[INFO] Switched to client_id #2 (def67890...)
[DEBUG] Making request to: ... with client_id: def67890...
```

### Исчерпание пула

```
[ERROR] All client IDs are exhausted!
[ERROR] API request failed: No active client IDs available
```

## Performance тесты

### Тест скорости переключения

Измерьте время от ошибки до успешного retry:
- Должно быть < 1 секунды
- Пользователь не должен заметить задержки

### Тест нагрузки

Отправьте 50+ треков подряд:
- Система должна справиться без утечек памяти
- Статистика должна корректно обновляться

## Мониторинг статистики

Добавьте временный endpoint для проверки статистики (в development окружении):

```python
# В handlers.py (временно для тестирования)
async def stats_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    settings = context.bot_data['settings']
    # Получить статистику из где-то (нужно сохранить ссылку на manager)
    stats = manager.get_stats()
    
    message = f"""
📊 Client ID Statistics:
Total: {stats['total']}
Active: {stats['active']}
Failed: {stats['failed']}
Cooldown: {stats['cooldown']}
Strategy: {stats['strategy']}
"""
    await update.message.reply_text(message)
```

## Checklist перед релизом

- [ ] Все unit тесты проходят
- [ ] Тестирование с реальным ботом успешно
- [ ] Backward compatibility проверена
- [ ] Failover работает корректно
- [ ] Round-robin работает корректно
- [ ] Cooldown механизм работает
- [ ] Логирование информативное и понятное
- [ ] Нет утечек памяти при длительной работе
- [ ] Документация актуальна
- [ ] .env.example обновлен

## Известные ограничения

1. **Нет автоматического scraping** - ID нужно добавлять вручную
2. **Нет персистентности** - статистика сбрасывается при перезапуске
3. **Нет уведомлений админа** - только логирование

## Будущие улучшения

- Автоматический scraper для получения новых ID
- Сохранение статистики в файл/БД
- Telegram уведомления админу при проблемах
- Веб-дашборд для мониторинга
- API для динамического добавления/удаления ID

## Troubleshooting

### Проблема: Тесты не запускаются

```bash
pip install pytest pytest-asyncio
```

### Проблема: Import errors

```bash
# Убедитесь что находитесь в корне проекта
pwd  # должно показать путь к scdownloadbot
python -c "import sys; print(sys.path)"
```

### Проблема: Логи не показывают client_id

Проверьте уровень логирования:
```env
LOG_LEVEL=DEBUG
```

## Контакты

При обнаружении багов создайте issue в репозитории с:
- Логами
- Конфигурацией (без реальных токенов!)
- Описанием проблемы
- Шагами для воспроизведения
