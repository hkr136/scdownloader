# 🔄 Автоматическая ротация Client ID

## Описание

Фича автоматической ротации SoundCloud Client ID позволяет боту автоматически переключаться между несколькими client_id при возникновении ошибок аутентификации (401/403). Это обеспечивает непрерывную работу бота даже если один или несколько client_id становятся неактивными.

## Преимущества

✅ **Автоматическая отказоустойчивость** - бот продолжает работать при падении одного ID  
✅ **Нулевое время простоя** - мгновенное переключение на резервный ID  
✅ **Обратная совместимость** - поддержка старого формата с одним ID  
✅ **Гибкие стратегии** - failover и round-robin ротация  
✅ **Умное восстановление** - автоматическая проверка "мертвых" ID после cooldown периода  

## Конфигурация

### Вариант 1: Один Client ID (обратная совместимость)

```env
SOUNDCLOUD_CLIENT_ID=your_client_id_here
```

### Вариант 2: Несколько Client ID (рекомендуется)

```env
# Несколько ID через запятую
SOUNDCLOUD_CLIENT_IDS=client_id_1,client_id_2,client_id_3

# Стратегия ротации (по умолчанию: failover)
CLIENT_ID_ROTATION_STRATEGY=failover

# Время cooldown для failed ID в секундах (по умолчанию: 300)
CLIENT_ID_COOLDOWN_SECONDS=300
```

## Стратегии ротации

### Failover (по умолчанию)

Использует один ID до тех пор, пока он не упадет, затем переключается на следующий.

**Когда использовать:**
- Для большинства случаев использования
- Когда хотите минимизировать количество используемых ID
- Когда важна простота и надежность

**Пример:**
```
ID1 (используется) → ID1 fails → ID2 (используется) → ID2 (используется)
```

### Round-Robin

Распределяет запросы равномерно между всеми активными ID.

**Когда использовать:**
- При высокой нагрузке на API
- Когда хотите распределить risk между несколькими ID
- Для снижения вероятности rate limiting на одном ID

**Пример:**
```
ID1 → ID2 → ID3 → ID1 → ID2 → ID3 ...
```

## Как это работает

### 1. Инициализация

При запуске бота создается `ClientIDManager` с пулом client_id из конфигурации.

### 2. Нормальная работа

- Бот использует текущий активный client_id для API запросов
- При успешных запросах ID помечается как успешный
- Статистика обновляется для мониторинга

### 3. Обработка ошибок

Когда возникает ошибка аутентификации (401 или 403):

1. **Client ID помечается как failed**
   - Статус меняется на `FAILED`
   - Счетчик ошибок увеличивается
   - Фиксируется время ошибки

2. **Автоматическое переключение**
   - Менеджер выбирает следующий активный ID
   - Запрос автоматически повторяется с новым ID
   - Пользователь не замечает проблемы

3. **Cooldown период**
   - Failed ID входит в cooldown на 5 минут (по умолчанию)
   - После cooldown ID может быть повторно протестирован
   - Если ID снова работает - восстанавливается статус ACTIVE

### 4. Логирование

Все события логируются для мониторинга:

```
[INFO] ClientIDManager: Using client_id: abc12345... (1/3 active)
[WARN] Authentication failed with client_id abc12345... (status: 401)
[INFO] Switched to client_id: def67890...
[INFO] Client ID def67890... marked as successful (total: 15)
```

## Статусы Client ID

- **ACTIVE** - ID работает нормально
- **FAILED** - ID не прошел аутентификацию (401/403)
- **COOLDOWN** - ID временно недоступен после ошибки (не 401/403)

## Мониторинг

### Получение статистики

ClientIDManager предоставляет метод `get_stats()` для мониторинга:

```python
stats = manager.get_stats()
# {
#     'total': 3,
#     'active': 2,
#     'failed': 1,
#     'cooldown': 0,
#     'strategy': 'failover',
#     'current_index': 1,
#     'client_ids': [...]
# }
```

### Health Check

Периодическая проверка здоровья всех ID:

```python
health = await manager.health_check()
```

## Примеры использования

### Пример 1: Базовая настройка с двумя ID

```env
SOUNDCLOUD_CLIENT_IDS=abc123,def456
CLIENT_ID_ROTATION_STRATEGY=failover
```

Бот будет использовать `abc123` пока он работает. При ошибке переключится на `def456`.

### Пример 2: Несколько ID с round-robin

```env
SOUNDCLOUD_CLIENT_IDS=id1,id2,id3,id4
CLIENT_ID_ROTATION_STRATEGY=round-robin
```

Запросы будут равномерно распределены между всеми 4 ID.

### Пример 3: Быстрое восстановление

```env
SOUNDCLOUD_CLIENT_IDS=id1,id2
CLIENT_ID_ROTATION_STRATEGY=failover
CLIENT_ID_COOLDOWN_SECONDS=60
```

Failed ID будет повторно проверен через 1 минуту вместо стандартных 5 минут.

## Получение нескольких Client ID

### Метод 1: Через браузер (несколько раз)

1. Откройте soundcloud.com в браузере
2. Откройте DevTools (F12) → Network
3. Воспроизведите трек
4. Найдите запрос к `api-v2.soundcloud.com`
5. Скопируйте `client_id` из URL
6. Очистите cookies/используйте режим инкогнито
7. Повторите процесс для получения другого ID

### Метод 2: Из разных источников

- Разные браузеры
- Разные устройства
- Мобильное приложение vs веб-версия
- Попросите друзей получить их client_id

### Метод 3: Сообщество

Поищите в сообществе SoundCloud разработчиков - иногда делятся рабочими ID.

## Troubleshooting

### Проблема: "No active client IDs available"

**Причина:** Все client_id в пуле стали неактивными.

**Решение:**
1. Проверьте логи - когда ID упали
2. Подождите cooldown период (5 минут по умолчанию)
3. Добавьте новые client_id в конфигурацию
4. Перезапустите бота

### Проблема: Client ID быстро становятся неактивными

**Причина:** ID могут быть заблокированы SoundCloud за частое использование.

**Решение:**
1. Увеличьте количество ID в пуле
2. Используйте round-robin стратегию
3. Увеличьте `RATE_LIMIT` для снижения частоты запросов
4. Регулярно обновляйте ID

### Проблема: Бот работает медленно

**Причина:** Частые переключения между ID при ошибках.

**Решение:**
1. Проверьте качество ваших client_id
2. Используйте только свежие, проверенные ID
3. Увеличьте cooldown период

## Best Practices

1. **Используйте минимум 3 client_id** для надежности
2. **Регулярно обновляйте ID** - раз в несколько недель
3. **Мониторьте логи** для раннего обнаружения проблем
4. **Используйте failover** для большинства случаев
5. **Храните запасные ID** на случай экстренной ситуации

## Миграция со старой версии

### Шаг 1: Обновите .env файл

Добавьте новые переменные (старый `SOUNDCLOUD_CLIENT_ID` продолжит работать):

```env
SOUNDCLOUD_CLIENT_IDS=your_id_1,your_id_2,your_id_3
```

### Шаг 2: Перезапустите бота

```bash
# Остановите текущий процесс
Ctrl+C

# Запустите снова
./run.sh
# или
python main.py
```

### Шаг 3: Проверьте логи

Убедитесь, что видите:
```
[INFO] ClientIDManager initialized with 3 client IDs, strategy: failover
```

## API Reference

### ClientIDManager

```python
manager = ClientIDManager(
    client_ids=['id1', 'id2'],
    strategy='failover',  # or 'round-robin'
    cooldown_seconds=300
)

# Получить активный ID
active_id = await manager.get_active_id()

# Пометить успешное использование
await manager.mark_success(active_id)

# Пометить ошибку
await manager.mark_failed(active_id, error_code=401)

# Получить статистику
stats = manager.get_stats()

# Health check
health = await manager.health_check()
```

## Архитектура

```
Settings (config/settings.py)
    ↓ (читает client_ids из .env)
ClientIDManager (api/client_id_manager.py)
    ↓ (управляет пулом ID)
SoundCloudClient (api/client.py)
    ↓ (использует ID для API запросов)
Handlers (bot/handlers.py)
    ↓ (обрабатывает запросы пользователей)
Telegram Bot
```

## Лицензия

Эта фича является частью SoundCloud Telegram Bot и распространяется под той же лицензией.
